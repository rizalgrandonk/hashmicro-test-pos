PORT=3636
SESSION_SECRET=replace-with-a-long-random-string

POSTGRES_USER=hashmicro_pos
POSTGRES_PASSWORD=replace-with-a-strong-password
POSTGRES_DB=hashmicro_pos

# For local dev — for Docker, the host is "postgres" (service name)
DATABASE_URL=postgresql://hashmicro_pos:replace-with-a-strong-password@localhost:5432/hashmicro_pos
