<nav class="navbar bg-base-100 shadow-sm px-4 sticky top-0 z-50">
  <div class="container mx-auto flex items-center justify-between">

    <a href="/" class="flex items-center gap-2 text-xl font-bold text-primary">
      HashMicro Test
    </a>

    <div class="flex items-center gap-1">
      <a href="/" class="btn btn-ghost btn-sm">Home</a>
      <% if (locals.user) { %>
        <a href="/products" class="btn btn-ghost btn-sm">Products</a>
        <a href="/character" class="btn btn-ghost btn-sm">Character Matcher</a>
        <a href="/pos" class="btn btn-ghost btn-sm">POS</a>
        <a href="/discounts" class="btn btn-ghost btn-sm">Discounts</a>
        <div class="divider divider-horizontal mx-1"></div>
        <div class="dropdown dropdown-end">
          <div tabindex="0" role="button" class="btn btn-ghost btn-sm gap-2">
            <div class="avatar avatar-placeholder">
              <div class="bg-primary text-primary-content rounded-full w-7">
                <span class="text-xs"><%= locals.user.name.charAt(0).toUpperCase() %></span>
              </div>
            </div>
            <%= locals.user.name %>
          </div>
          <ul tabindex="0" class="dropdown-content menu bg-base-100 rounded-box z-1 w-40 p-2 shadow-md border border-base-200">
            <li>
              <form action="/logout" method="POST">
                <button type="submit" class="text-error w-full text-left">Logout</button>
              </form>
            </li>
          </ul>
        </div>
      <% } else { %>
        <a href="/login" class="btn btn-primary btn-sm">Login</a>
      <% } %>
    </div>

  </div>
</nav>